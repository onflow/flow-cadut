import { basicTemplate, basic } from "./basic";
	import { logTemplate, log } from "./log";
	import { metadataTemplate, metadata } from "./metadata";
	import { panicTemplate, panic } from "./panic";
	export { basicTemplate, basic, logTemplate, log, metadataTemplate, metadata, panicTemplate, panic };
	export default {
	  basicTemplate,
	  basic,
	  logTemplate,
	  log,
	  metadataTemplate,
	  metadata,
	  panicTemplate,
	  panic,
	};
	