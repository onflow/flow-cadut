import { logTemplate, log } from "./log";
	import { panicTemplate, panic } from "./panic";
	export { logTemplate, log, panicTemplate, panic };
	export default { logTemplate, log, panicTemplate, panic };
	